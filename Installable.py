class Installable:
    def __init__(self, package_name, package_version, install_type):
        self.package_name = package_name
        self.package_version = package_version
        self.install_type = install_type
        